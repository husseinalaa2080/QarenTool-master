<%= form_with(model: user, url: admin_user_path, local: true, class: 'two-thirds-width') do |form| %>
  <% if user.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(user.errors.count, "error") %> prohibited this user from being saved:</h2>

      <ul>
      <% user.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="form-group row">
    <%= form.label :name, class: 'col-md-2 text-center' %>
    <%= form.text_field :name, class: 'form-control col' %>
  </div>

  <div class="form-group row">
    <%= form.label :mobile, class: 'col-md-2 text-center' %>
    <%= form.text_field :mobile, class: 'form-control col' %>
  </div>

  <div class="form-group row">
    <%= form.label :email, class: 'col-md-2 text-center' %>
    <%= form.text_field :email, class: 'form-control col' %>
  </div>

  <div class="actions form-group text-center">
    <%= form.submit class: 'btn btn-outline-info' %>
  </div>
<% end %>
